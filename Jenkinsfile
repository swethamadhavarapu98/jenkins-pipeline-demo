pipeline {
    agent any

    environment {
        IMAGE_NAME = 'swethamadhavarapu/jenkins-pipeline-demo'
        IMAGE_TAG = 'latest'
    }

    stages {
        stage('Checkout') {
            steps {
                echo 'Code checked out from GitHub'
            }